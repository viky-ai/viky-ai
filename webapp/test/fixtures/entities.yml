weather_sunny:
  solution: 'weather: sunny'
  auto_solution_enabled: true
  terms: <%= [{term: "soleil", locale: "fr"}, {term: "sun", locale: "en"}].to_json.inspect %>
  entities_list: weather_conditions
  position: 1

weather_raining:
  solution: 'weather: raining'
  auto_solution_enabled: true
  terms: <%= [{term: "pluie", locale: "fr"}, {term: "rain", locale: "en"}].to_json.inspect %>
  entities_list: weather_conditions
  position: 0

weather_dates_today:
  solution: 'date: today'
  auto_solution_enabled: true
  terms: <%= [{term: "aujourd'hui", locale: "fr"},{term: "tout à l'heure", locale: "fr"}, {term: "today", locale: "en"}].to_json.inspect %>
  entities_list: weather_dates
  position: 1

weather_dates_tomorrow:
  solution: 'date: tomorrow'
  auto_solution_enabled: true
  terms: <%= [{term: "tomorrow", locale: "*"}].to_json.inspect %>
  entities_list: weather_dates
  position: 0
