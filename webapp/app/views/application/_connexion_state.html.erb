<% if user_signed_in? %>
  <%= link_to t('views.nav.manage_agents_html'), agents_path %>
<% else %>
  <% unless Feature.user_registration_disabled? %>
    <%= link_to t('views.nav.sign_up'), new_user_registration_path %>
  <% end %>
  <%= link_to t('views.nav.sign_in'), new_user_session_path %>
<% end %>
