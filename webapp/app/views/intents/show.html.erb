<header class="header">
  <div class="header__breadcrumb">
    <%= link_to t('views.agents.breadcrumb.base'), agents_path %> /
    <%= link_to @agent.name, user_agent_path(@agent.owner, @agent) %> /
    <strong>
      <%= @intent.intentname %>
    </strong>
    (<%= @agent.owner.username %>/<%= @agent.agentname %>/<%= @intent.intentname %>)
  </div>
</header>

<% content_for :right_aside do %>
  <%= render '/console/base' %>
<% end %>

<%= render 'interpretations/new', locale: @locale %>
