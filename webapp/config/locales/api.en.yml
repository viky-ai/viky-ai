en:
  controllers:
    api:
      format_not_acceptable: "Format not acceptable. JSON only is accepted."
      access_denied: "Access denied: wrong token."
  nlp:
    interpret:
      invalid_agent_token: is not valid
      invalid_ownername: is incorrect, '%{ownername}' user doesn't exist
      invalid_agentname: is incorrect, '%{ownername}' user doesn't own '%{agentname}' agent
