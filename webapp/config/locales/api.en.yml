en:
  controllers:
    api:
      format_not_acceptable: "Format not acceptable. JSON only is accepted."
      v1:
        nls:
          interpret:
            wrong_agent_token: "Wrong token for agent %{agent_name}."
