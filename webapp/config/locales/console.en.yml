en:
  views:
    console:
      title: Console
      sentence_placeholder: Say something...
      tabs:
        explain: Explain
        json: JSON
      error_occurred: Oops, an error occurred.
      no_intent_found: No interpretation found.
      intent_found:
        one: "1 interpretation found."
        other: "%{count} interpretations found."
      warnings:
        one: "1 Warning"
        other: "%{count} Warnings"
      verbose:
        label: "Verbose"
        enable: "ON"
        disable: "OFF"
      datetime:
        label: "Now"
        auto: "Auto"
        manual: "Manual"
        addon: "Now (ISO 8601)"
      js_error:
        message_html: >
          <p>
            It's a JavaScript error related to the evaluation of a solution.
          </p>
          <p>
            The JavaScript runtime returns the following error:<br />
            <code>%{code}</code>
          </p>
        btn: See the offending code
        context_intro: "JavaScript execution context:"
        note_html: >
          Note: viky.ai uses <a target="_blank" href="https://duktape.org/">Duktape 2.4</a>
          JavaScript runtime which support ECMAScript E5/E5.1
      go_to_api_endpoint: Go to API endpoint
      download_package: Download package
      fullscreen_enter: Enter in fullscreen mode
      fullscreen_leave: Leave fullscreen mode
      expression_text: Expression
      scores: Scores
      highlight: Highlight
      solution: Solution
      reset:
        title: Reset console to the current agent
      no_access: You do not have access to this agent.
      regression_check:
        add: "Add to tests suite"
        update: Update
        sentence_too_long:
          Adding this to the tests suite is not possible because the text to
          analyze exceeds 200 characters ({{test.sentence.length}} characters).
        summary:
          tests_suite: Tests suite
          tests:
            zero: "0 test"
            one: "1 test"
            other: "%{count} tests"
          running:
            zero: "0 running"
            one: "1 running"
            other: "%{count} running"
          success:
            zero: "0 success"
            one: "1 success"
            other: "%{count} success"
          failures:
            zero: "0 failure"
            one: "1 failure"
            other: "%{count} failures"
